import { MyAppDirective } from './my-app.directive';

describe('MyAppDirective', () => {
  it('should create an instance', () => {
    const directive = new MyAppDirective();
    expect(directive).toBeTruthy();
  });
});
